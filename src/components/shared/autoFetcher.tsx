import readExcelAndSendToApi from '@/lib/extractExcel';
import React from 'react'

interface file{
    url:string
}

export default async function AutoFetcher(props:file) {
    
return (
    <div></div>
  )
}
